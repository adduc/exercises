# Terraform cache
.terraform

# Terraform state
terraform.tfstate
terraform.tfstate.*.backup
terraform.tfstate.backup
.terraform.tfstate.lock.info

# Terraform lock file (for provider versions)
.terraform.lock.hcl

# Terraform environment configuration
terraform.tfvars

# Kubeconfig file (generated by k3s)
kubeconfig.yaml

# GitLab Personal Access Token for root user (if used in the setup)
root_access_token.txt
