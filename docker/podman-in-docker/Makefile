up:
	docker compose up -d

shell:
	docker compose exec podman bash

demo:
	docker compose exec podman podman run --rm docker.io/library/alpine:3.20 echo "hello from nested podman"

down:
	docker compose down --timeout 0
